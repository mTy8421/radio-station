# ระบบวิทยุออนไลน์ AutoDJ (Icecast + FastAPI)

โปรเจกต์นี้เป็นระบบจำลองสถานีวิทยุออนไลน์ที่มาพร้อมกับฟีเจอร์ AutoDJ (ดีเจเปิดเพลงอัตโนมัติ) ซึ่งพัฒนาด้วย **Python (FastAPI)** และใช้ **Icecast** เป็นเซิร์ฟเวอร์กระจายสัญญาณเสียง

## 🚀 คุณสมบัติเด่น (Features)

*   **Icecast Server**: เซิร์ฟเวอร์มาตรฐานสำหรับการสตรีมเสียงแบบ Real-time
*   **AutoDJ System**: ระบบเล่นเพลงอัตโนมัติใน Background ไม่ต้องเปิดเพลงเอง
*   **Smart Scheduling**: สามารถกำหนดช่วงเวลาเล่นของแต่ละเพลงได้ (เช่น ให้เล่นเฉพาะช่วง 08:00 - 10:00 น.)
*   **RESTful API**: จัดการเพลงได้ง่ายๆ ผ่าน API (อัปโหลด, ดูรายการ, ลบเพลง)
*   **Docker Support**: ติดตั้งและรันทุกอย่างได้ทันทีด้วย Docker Compose

## 🛠️ สิ่งที่ต้องมี (Prerequisites)

*   [Docker](https://www.docker.com/)
*   [Docker Compose](https://docs.docker.com/compose/)

## 📦 การติดตั้งและเริ่มใช้งาน (Installation)

1.  **Clone โปรเจกต์** หรือดาวน์โหลดไฟล์ลงเครื่อง
2.  **รัน Docker Compose** เพื่อเริ่มระบบ:
    ```bash
    docker-compose up -d --build
    ```
3.  รอสักครู่เพื่อให้ระบบ Initial และเริ่มทำงาน

## 🎧 การใช้งาน (Usage)

หลังจากรันระบบแล้ว สามารถเข้าถึงบริการต่างๆ ได้ดังนี้:

### 1. ฟังเพลง (Radio Stream)
*   URL สำหรับฟังเพลง: `http://localhost:8000/stream`
*   หน้า Admin ของ Icecast: `http://localhost:8000`
    *   **Username**: `admin`
    *   **Password**: `adminhackme` (แก้ไขได้ใน `docker-compose.yml`)

### 2. จัดการเพลง (AutoDJ API)
เข้าใช้งานหน้า Swagger UI เพื่อทดลองยิง API ได้ที่:
👉 **http://localhost:8080/docs**

#### รายละเอียด API Endpoint:

*   **POST `/upload`**: อัปโหลดเพลงเข้าสู่ระบบ
    *   `file`: เลือกไฟล์ .mp3
    *   `start_hour`: (ตัวเลือก) ชั่วโมงเริ่มต้นที่ให้เล่นเพลงนี้ (0-23)
    *   `end_hour`: (ตัวเลือก) ชั่วโมงสิ้นสุดที่ให้เล่นเพลงนี้ (0-23)
    *   *หมายเหตุ: หากไม่กำหนดเวลา เพลงจะถูกสุ่มเล่นได้ตลอดเวลา*

*   **GET `/playlist`**: ดูรายชื่อเพลงและตารางเวลาทั้งหมดในระบบ

*   **DELETE `/delete/{filename}`**: ลบไฟล์เพลงออกจากระบบ

## 📂 โครงสร้างไฟล์ (Project Structure)

```
.
├── app/
│   ├── main.py          # โค้ดหลักของ AutoDJ (FastAPI + Streaming Logic)
│   ├── Dockerfile       # การตั้งค่า Environment สำหรับ Python
│   └── requirements.txt # รายชื่อ Library ที่ใช้
├── music/               # โฟลเดอร์สำหรับเก็บไฟล์เพลง (ถูก Mount volume ไว้)
├── docker-compose.yml   # การตั้งค่า Container ของ Icecast และ AutoDJ
└── README.md            # คู่มือการใช้งาน
```

## ⚙️ การตั้งค่าเพิ่มเติม (Configuration)

คุณสามารถแก้ไขค่าต่างๆ ได้ในไฟล์ `docker-compose.yml`:
*   `ICECAST_SOURCE_PASSWORD`: รหัสผ่านสำหรับ AutoDJ เชื่อมต่อ
*   `ICECAST_ADMIN_PASSWORD`: รหัสผ่าน Admin หน้าเว็บ Icecast
*   `ICECAST_PASSWORD`: รหัสผ่านสำหรับผู้ฟัง (ถ้าต้องการจำกัดการเข้าถึง)
